export type ChatRoomAPI = {
  chatRoomId: string;
  festivalId: string;
  userId: string;
  title: string;
  // currentParticipants: number;
  // maxParticipants: number;
  // radius: number;
  // lat: number;
  // lon: number;
};

